# monitoring/prometheus.yml
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'gestionproduits-api'
    static_configs:
      - targets: ['host.docker.internal:5167']  # ← ton port dotnet run
    metrics_path: /metrics